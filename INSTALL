Fusilli uses libstartup-notification which is available at
ftp://ftp.gnome.org/pub/GNOME/sources/startup-notification/

Fusilli uses automake, in order to generate the Makefiles for Fusilli use:

	$ autogen.sh

After that, standard build procedures apply:

	$ make
	# make install

